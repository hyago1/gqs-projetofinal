

# Cria as pastas dentro de 'src/'
mkdir -p src/{controllers,models,routes,views,public,database}

# Cria as pastas dentro de 'tests/'
mkdir -p tests/{unit,integration}

# Cria as pastas dentro de 'cypress/'
mkdir -p cypress/e2e